# Security

## 기본 원칙
- 비밀키는 코드/문서/커밋 메시지에 직접 노출하지 않습니다.
- 연동 키는 `/settings`에서만 관리하고, 화면에는 마스킹 상태로 표시합니다.

## 운영 수칙
1. OAuth 토큰 만료/오류는 즉시 갱신
2. 불필요한 권한(scope) 제거
3. 로그 공유 시 민감값 제거
4. 관리자 설정은 `/admin`에서만 변경

## 점검 주기
- 일간: Ops Monitor 알림 확인
- 주간: 연동 권한/토큰 상태 검토
